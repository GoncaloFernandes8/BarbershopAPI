🇵🇹
💈 Barbearia — App de Marcação (Angular + Spring Boot)

Aplicação completa de marcações para barbearia: frontend em Angular (standalone + signals) e backend em Spring Boot com PostgreSQL.
Permite escolher serviço e barbeiro, ver disponibilidade em tempo real, reservar e gerir horários.

Demo ao vivo

Frontend (Vercel): https://barbershop-frontend-nine.vercel.app

API (Koyeb): https://due-constancia-goncalo-6b7726ec.koyeb.app

(nota: no plano gratuito a API pode “adormecer”; a primeira chamada pode demorar alguns segundos a acordar)

✨ Funcionalidades
👤 Público / Cliente

Explorar serviços com duração, preço e imagens ilustrativas.

Escolher barbeiro ativo.

Calendário mensal + slots de horas gerados pela API (disponibilidade real).

Marcação com notas opcionais.

Página de sucesso com o ID da marcação.

Conta de utilizador: registo por email (token/verify), login (JWT), sessão e logout.

✂️ Backoffice (operacional via API; UI própria em progresso)

Gestão de serviços (/services): criar, editar, ativar/desativar.

Gestão de barbeiros (/barbers): criar, editar, ativar/desativar.

Horário de trabalho (/working-hours): definir por dia da semana e intervalos.

Ausências/Férias (/time-off): bloquear períodos.

Agenda (/appointments): listar por barbeiro/intervalo, criar (via front), cancelar.

🗺️ Extras de UX/UI

Layout 100% responsivo (mobile-first), com menu “hambúrguer” acessível.

Animações subtis e cartões com iluminação/gradientes (CSS puro).

Google Maps embebido na Home.

Otimizações mobile: sem zoom automático em inputs, tipografia adaptativa.

🧱 Tecnologias

Frontend

Angular 17+ (standalone components, signals, RxJS)

Forms, Router

CSS moderno (grid/flex, clamp(), color-mix(), mask-composite)

Google Maps embebido

Backend

Spring Boot 3 (Web, Validation)

Spring Data JPA / Hibernate

PostgreSQL (Neon) + HikariCP

CORS configurável via application-prod.yml

Autenticação básica/JWT (esqueleto), verificação de registo por email SMTP

Flyway opcional (não incluído neste repo)

Infra

Vercel (frontend)

Koyeb (API) — cold start no plano gratuito

Gmail SMTP para envio de emails (registo/verify)

O frontend nunca “calcula” disponibilidade; pede à API:

/availability?barberId=&serviceId=&date= → devolve OffsetDateTime[]

A criação da marcação valida conflitos e calcula o fim do serviço no servidor.

🔌 API (resumo de endpoints)

Auth /auth/register, /auth/verify, /auth/login

Availability /availability?barberId=&serviceId=&date=YYYY-MM-DD

Appointments

POST /appointments (criar)

GET /appointments?barberId=&from=&to= (listar)

PATCH /appointments/{id}/cancel

GET /appointments/{id}

Barbers GET/POST/PUT/DELETE /barbers[/id]

Services GET/POST/PUT/DELETE /services[/id]

Working Hours GET /working-hours?barberId=, POST /working-hours, DELETE /working-hours/{id}

Time Off GET /time-off?barberId=&from=&to=, POST /time-off, DELETE /time-off/{id}

Clients GET/POST/PUT/DELETE /clients[/id]

DTOs/entidades incluídos no projeto; erros 404 → ResourceNotFoundException.


🇬🇧
💈 Barbershop — Booking App (Angular + Spring Boot)

Full barbershop booking application: Angular (standalone + signals) frontend and Spring Boot backend with PostgreSQL.
Lets you choose a service and barber, see real-time availability, book, and manage schedules.

Live demo

Frontend (Vercel): https://barbershop-frontend-nine.vercel.app

API (Koyeb): https://due-constancia-goncalo-6b7726ec.koyeb.app

(note: on the free plan the API may “sleep”; the first call can take a few seconds to wake it up)

✨ Features
👤 Public / Client

Browse services with duration, price, and illustrative images.

Choose an active barber.

Monthly calendar + time slots generated by the API (real availability).

Booking with optional notes.

Success page with the booking ID.

User account: email sign-up (token/verify), login (JWT), session, and logout.

✂️ Back office (operational via API; dedicated UI in progress)

Service management (/services): create, edit, enable/disable.

Barber management (/barbers): create, edit, enable/disable.

Working hours (/working-hours): define by weekday and time ranges.

Absences/Vacation (/time-off): block periods.

Schedule (/appointments): list by barber/time range, create (via front), cancel.

🗺️ UX/UI Extras

100% responsive layout (mobile-first) with an accessible hamburger menu.

Subtle animations and cards with lighting/gradients (pure CSS).

Google Maps embed on the Home page.

Mobile optimizations: no automatic zoom on inputs, adaptive typography.

🧱 Tech Stack

Frontend

Angular 17+ (standalone components, signals, RxJS)

Forms, Router

Modern CSS (grid/flex, clamp(), color-mix(), mask-composite)

Google Maps embed

Backend

Spring Boot 3 (Web, Validation)

Spring Data JPA / Hibernate

PostgreSQL (Neon) + HikariCP

CORS configurable via application-prod.yml

Basic/JWT authentication (skeleton), sign-up verification via SMTP email

Optional Flyway (not included in this repo)

Infra

Vercel (frontend)

Koyeb (API) — cold start on the free plan

Gmail SMTP for sending emails (sign-up/verify)

The frontend never “calculates” availability; it asks the API:

/availability?barberId=&serviceId=&date= → returns OffsetDateTime[]

Booking creation validates conflicts and computes the service end on the server.

🔌 API (endpoint summary)

Auth /auth/register, /auth/verify, /auth/login

Availability /availability?barberId=&serviceId=&date=YYYY-MM-DD

Appointments

POST /appointments (create)

GET /appointments?barberId=&from=&to= (list)

PATCH /appointments/{id}/cancel

GET /appointments/{id}

Barbers GET/POST/PUT/DELETE /barbers[/id]

Services GET/POST/PUT/DELETE /services[/id]

Working Hours GET /working-hours?barberId=, POST /working-hours, DELETE /working-hours/{id}

Time Off GET /time-off?barberId=&from=&to=, POST /time-off, DELETE /time-off/{id}

Clients GET/POST/PUT/DELETE /clients[/id]

DTOs/entities included in the project; 404 errors → ResourceNotFoundException.



